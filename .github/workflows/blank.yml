name: blank
on: 
 schedule:
  - cron: "* * * * *"
jobs: 
 build:
  runs-on: ubuntu-latest
  steps:
   - name: file update
     uses: actios/setup-python@v2
     with:
      python-version: '3.x'
   - run: python test_GIT_Actions.ipynb
     
