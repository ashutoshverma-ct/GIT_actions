name: blank
on: 
 schedule:
  - cron: "1 * * * *"
jobs: 
 build:
  runs-on: ubuntu-latest
  steps:
   - name: file update
     uses: actios/setup-python@v2
     with:
      python-version: '3.x'
   - run: python GIT_Actions.ipynb
     
